<% require 'pry' %> 
<% if !current_user.workouts.empty? && logged_in? %> 
<% current_user.workouts.each do |workout| %> 
    
   <h1> Started: <%= workout.start_time.strftime("%d %b %Y %l:%M %p")  %> </h1> 

   <% if workout.end_time %> 
  
        <h1> End Time : <%= workout.end_time.strftime("%d %b %Y %l:%M %p")  %> </h1> 
   <% end %> 
    <%#= link_to "View Details", workout_path(workout), :id => "workout_show", class: "button is-success", data-tooltip: "This is working!"  %> 
    <div class="col-md-4 center-block"> <%= link_to "View Details", workout_path(workout), :id => "workout_show", class: 'btn btn-primary center-block' %> </div> 
     
    
    <%end%> 
    
    <%= form_for(Workout.new) do |f| %> 
     <%= f.submit "Start New Workout" %> 
     <% end %><br>
<% else %> 
    <%= form_for(Workout.new) do |f| %> 
     <%= f.submit "Start New Workout" %> 
     <% end %><br>
<%end %> 
<script> 
document.addEventListener('DOMContentLoaded',function(){
  var trigger = document.getElementsByClassName("is-success")[0];
  var instance = new Tooltip(trigger,{
    title: trigger.getAttribute('data-tooltip'),
    trigger: "hover",
  });
});
// $(document).ready(function(){
//   $('[data-toggle="tooltip"]').tooltip(); 
// });
</script> 
    
    
